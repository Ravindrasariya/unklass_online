kind = "web"
previewPath = "/"
title = "Unklass"
version = "1.0.0"
id = "artifacts/unklass"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 19325

[services.development]
run = "pnpm --filter @workspace/unklass run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/unklass", "run", "build" ]
publicDir = "artifacts/unklass/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "19325"
BASE_PATH = "/"
